name: provider-pinecone-native
runtime: yaml
plugins:
  providers:
    - name: pinecone
      path: ../../bin

resources:
  myRandomResource:
    type: xyz:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
